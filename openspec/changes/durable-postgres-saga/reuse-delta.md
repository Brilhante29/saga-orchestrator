# Reuse Delta

Record for `portfolio-reuse-kit`:

- durable claims require an infrastructure failure test;
- saga guidance must require `FAILED` on compensation failure;
- V2 producers need a reusable image/dependency/source provenance collector.

Resource-specific saga SQL remains project-owned.
