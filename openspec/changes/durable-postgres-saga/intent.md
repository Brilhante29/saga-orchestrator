# Intent

Replace the in-memory saga simulator with durable, restart-safe orchestration whose operations and compensations are idempotent in PostgreSQL.

Success means failures are observable, recovery is repeatable, and the public benchmark is generated by the current implementation.
